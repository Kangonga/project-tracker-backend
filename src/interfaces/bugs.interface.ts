export interface Bug {
  title: string;
  description: string;
  status: string;
  priority: string;
  reportedById: string;
  projectId: string;
  assigneeId: string;
  closedById: string;
  createdAt: string;
  closedAt: string;
}
